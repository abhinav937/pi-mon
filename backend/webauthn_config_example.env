# WebAuthn Configuration Example for Production
# Copy this file to .env and update with your actual values

# REQUIRED: Set your actual domain (e.g., 'example.com' not 'https://example.com')
# This should be the effective domain without protocol or subdomain
WEBAUTHN_RP_ID=example.com

# REQUIRED: Set your full origin URL (e.g., 'https://app.example.com')
# This should include the protocol and full hostname
WEBAUTHN_ORIGIN=https://app.example.com

# REQUIRED: Set a strong, random JWT secret for production
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here

# OPTIONAL: JWT token expiration in hours (default: 24)
JWT_EXPIRY_HOURS=24

# OPTIONAL: Challenge expiration in seconds (default: 600 = 10 minutes)
WEBAUTHN_CHALLENGE_EXPIRY=600

# OPTIONAL: User verification requirement
# Options: REQUIRED, PREFERRED, DISCOURAGED
WEBAUTHN_USER_VERIFICATION=PREFERRED

# OPTIONAL: Supported algorithms (comma-separated)
# Default: ECDSA_SHA_256,RSASSA_PKCS1_v1_5_SHA_256
WEBAUTHN_ALGORITHMS=ECDSA_SHA_256,RSASSA_PKCS1_v1_5_SHA_256

# OPTIONAL: Supported transports (comma-separated)
# Default: USB,NFC,BLE,INTERNAL,HYBRID
WEBAUTHN_TRANSPORTS=USB,NFC,BLE,INTERNAL,HYBRID
